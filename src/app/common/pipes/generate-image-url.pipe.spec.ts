import { GenerateImageUrlPipe } from './generate-image-url.pipe';

describe('GenerateImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
