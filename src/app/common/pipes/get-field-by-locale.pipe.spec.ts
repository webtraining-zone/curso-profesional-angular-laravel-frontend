import { GetFieldByLocalePipe } from './get-field-by-locale.pipe';

describe('GetFieldByLocalePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFieldByLocalePipe();
    expect(pipe).toBeTruthy();
  });
});
