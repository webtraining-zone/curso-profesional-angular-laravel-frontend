import { GetFirstCharPipe } from './get-first-char.pipe';

describe('GetFirstCharPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstCharPipe();
    expect(pipe).toBeTruthy();
  });
});
